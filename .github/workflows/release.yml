# Build and publish a GitHub Release when you push a version tag: v1.2.3
# The tag must point to a commit on main (see "Require tag on main" step).
# Asset: one zip with universal (arm64+x86_64) Notch Pocket.app, ad-hoc signed.

name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  publish:
    runs-on: macos-14
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Require tag on main
        run: |
          set -euo pipefail
          git fetch origin main
          if ! git merge-base --is-ancestor "${GITHUB_SHA}" "origin/main"; then
            echo "::error::Release tags must point to a commit on main (the tagged commit must be an ancestor of origin/main). Merge to main first, then tag and push." >&2
            exit 1
          fi

      - name: Parse version from tag
        id: ver
        run: |
          TAG="${GITHUB_REF_NAME}"
          VERSION="${TAG#v}"
          if [[ -z "${VERSION}" || "${VERSION}" == "${TAG}" ]]; then
            echo "error: expected tag like v1.2.3, got: ${TAG}" >&2
            exit 1
          fi
          echo "version=${VERSION}" >> "${GITHUB_OUTPUT}"

      - name: Build universal .app
        env:
          VERSION_OVERRIDE: ${{ steps.ver.outputs.version }}
        run: ./release-package.sh

      - name: Zip app for release asset
        env:
          VERSION: ${{ steps.ver.outputs.version }}
        run: |
          set -euo pipefail
          APP="Notch Pocket-${VERSION}.app"
          OUT="Notch-Pocket-${VERSION}.zip"
          cd releases
          zip -r -y "../${OUT}" "${APP}"
          cd ..
          ls -la "${OUT}"

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: Notch-Pocket-${{ steps.ver.outputs.version }}.zip
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
